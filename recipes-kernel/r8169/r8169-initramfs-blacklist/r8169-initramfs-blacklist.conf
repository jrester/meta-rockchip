# ROCK 5T RTL8125B probing in initramfs can trigger the early udev/active-root
# crash path when no Ethernet link is present. Suppress alias autoload here;
# the rootfs r8169-delayed-load.service explicitly loads r8169 later.
blacklist r8169
