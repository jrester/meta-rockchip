# ROCK 5T has two RTL8125B Ethernet controllers handled by r8169.
# Let the board reach root/userspace before probing them; r8169-delayed-load.service
# explicitly loads the module later so one image can support Ethernet and WiFi.
blacklist r8169
